extends ../repository/repository.pug

block content

  mixin userCard(user)
    div
      img(src=avatar)
      h3 !{user.login}


    div.rep-list
    if users
      each user in users
        +userCard(rep)
    else
      h3 Пока подпискок нет!