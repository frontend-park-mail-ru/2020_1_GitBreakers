

div.section
  div.section__main
    div.profile
      div.profile_col1
        div.profile-info
          div.card
            h2.title Профиль
              hr.line-separator.line-separator_extra-thin
              img.profile__avatar(src=image alt='avatar')
              h3.profile-info__login Логин: !{login}
              h3.profile-info__email Email: !{email}
              div.profile-info__button
                if (auth === login)
                  button.route.button.button_long(type='submit' name='settings' data-section='/settings') Изменить профиль
      div.profile_col2
        div.profile__data
          div.card.col2-height
            div.profile__data__top
              div.profile__data__top__menu
                .profile__data__top__menu_item
                  a.route.profile__menu__link(href='' data-section=`/profile/${login}`) Репозитории
                .profile__data__top__menu_item
                  a.route.profile__menu__link(href='' data-section=`/profile/${login}/stars`) Звезды !{Stars}


                if (auth === login)
                  .profile__data__top__menu_item
                    a.route.profile__menu__link(href='' data-section=`/user/${login}/pull_requests`) Пулл реквесты
                  div.profile__data__top__menu_item
                    button.route.button(type='submit' data-section='/new') Создать репозиторий
            hr.line-separator.line-separator_thin
            div.profile__data__main
              block content
